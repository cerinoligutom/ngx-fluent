import { NgxFluentPipe } from './ngx-fluent.pipe';

describe('NgxFluentPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxFluentPipe();
    expect(pipe).toBeTruthy();
  });
});
